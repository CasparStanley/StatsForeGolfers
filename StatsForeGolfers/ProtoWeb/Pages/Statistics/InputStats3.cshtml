@page
@model ProtoWeb.Pages.Statistics.InputStats3Model
@{
    ViewData["Title"] = "InputStats3";
}

<div class="row justify-content-center p-4">
    <div class="col-auto">
        <h1> Hole @Model.CurrentHoleId</h1>
        <div class="my-auto container frame">
            <form method="post">
                <table class="text-center">
                    <tr>
                        <td colspan="3">Scramble</td>
                    </tr>
                    <tr>
                        <td>Out</td>
                        <td>In</td>
                    </tr>
                    <tr>
                        <td class="p-2">
                            <input asp-page-handler="ScrambleOut" type="submit" class="btn-danger btn-miss" value="Out" />
                        </td>
                        <td class="p-2">
                            <input asp-page-handler="ScrambleIn" type="submit" class="btn-primary btn-hit" value="In" />
                        </td>
                    </tr>
                </table>
            </form>
        </div>
        <br />
        <div class="my-auto container frame">
            <p class="text-dark">
                Debug:
            </p>
            <p class="text-dark">
                Fairway misses left: @Model.MockSheet.FairWayMissLeft
            </p>
            <p class="text-dark">
                Fairway hits: @Model.MockSheet.FairWayHit
            </p>
            <p class="text-dark">
                Fairway misses right: @Model.MockSheet.FairWayMissRight
            </p>
            <br />
            <p class="text-dark">
                Green misses left: @Model.MockSheet.GreenMissLeft
            </p>
            <p class="text-dark">
                Green hits: @Model.MockSheet.GreenHit
            </p>
            <p class="text-dark">
                Green misses right: @Model.MockSheet.GreenMissRight
            </p>
        </div>
    </div>
</div>